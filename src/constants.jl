const RS_PROBS = [:rs78, :rs84] # RockSample problems we have polcies for
const TG_PROBS = [:tag] # Tag problems we have polcies for
const AGENTS = [:normal, :perfect, :random, :naive, :scaled, :noisy] # implemented agents
